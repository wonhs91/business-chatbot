{
  "name": "business-chatbot-widget",
  "version": "0.1.0",
  "description": "Embeddable chat widget for the business chatbot backend.",
  "scripts": {
    "dev": "tsup src/index.ts --format iife --sourcemap --watch --global-name=BusinessChatWidget",
    "build": "tsup src/index.ts --format iife --minify --dts=false --global-name=BusinessChatWidget",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.8.0",
    "@typescript-eslint/parser": "^7.8.0",
    "eslint": "^9.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
